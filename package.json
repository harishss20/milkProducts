{
  "name": "e-com",
  "version": "1.0.0",
  "description": "E-commerce application for product catalog",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && concurrently \"npm run server\" \"npm run client\"",
    "server": "cd Server && npm install && nodemon main.js",
    "client": "cd client && npm install && npm start",
    "build": "cd client && npm run build"
  },
  "keywords": [
    "e-commerce",
    "catalog",
    "fullstack"
  ],
  "author": "Your Name",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
