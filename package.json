{
  "name": "e-com",
  "version": "1.0.0",
  "description": "E-commerce application for product catalog",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm install && npm start",
    "client": "cd client && npm install && npm start",
    "build": "cd client && npm run build"
  },
  "keywords": [
    "e-commerce",
    "catalog",
    "fullstack"
  ],
  "author": "Your Name",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
